# --- 핵심 통신 및 환경 설정 ---
requests>=2.31.0
python-dotenv>=1.0.1

# --- 데이터 수집 및 분석 ---
# pykrx 는 Windows 전용 의존성 문제로 제외, yfinance 로 대체
yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0      # parquet 캐시 저장용 (pandas 의존)

# --- 최적화 및 시각화 ---
scipy>=1.10.0
matplotlib>=3.7.0
